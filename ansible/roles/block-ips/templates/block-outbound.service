[Unit]
Description=Script that fetches IP lists from URLs and blocks outbound packets to them
After=network.target

[Service]
UMask=022
Type=simple
User=root
Group=root
ExecStart=/usr/local/bin/block-outbound.sh
MemoryLimit=1G
Restart=no

[Install]
WantedBy=multi-user.target